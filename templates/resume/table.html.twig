<table class="table text-center">
    <thead>
    <tr>
        <th class="active text-center">{{ 'applicant'|trans }}</th>
        <th class="active text-center">{{ 'about.me'|trans }}</th>
    </tr>
    </thead>

    <tbody>
    {% for resume in resumes %}
        <tr>
            <td>
                <a href="{{ path('resume_show', {id: resume.id}) }}">
                    {{ resume. title }}
                </a>
            </td>
            <td>{{ resume.aboutMe }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>