{{ form_start(search_form,{
    'attr': {'class': 'navbar-form navbar-center','action':path('category_show', { 'slug': slug })}
}) }}

    {{ form_widget(search_form.search,{
        'attr': {'class': 'form-control search-query'}
    }) }}

{{ form_end(search_form) }}